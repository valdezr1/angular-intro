import { Component, OnInit } from '@angular/core';

// This component was generated through using the CLI
// The CLI automatically concatinates "Component" to the
// component name. Here's the command: 
// ng generate component {directoryStartingFromApp}<componentName>
// This command can be abreviatted by replacing the "generate" to 'g'
// and/or the "component" to 'c'
// This will automatically add a .html, .css, .ts, and .spec.ts file.
// You can add the flag "--spec=false" to not create the .spec.ts file.

@Component({
  selector: 'app-test',
  templateUrl: './test.component.html',
  styleUrls: ['./test.component.css']
})
export class TestComponent implements OnInit {

  constructor() { }

  ngOnInit() {
  }

}
